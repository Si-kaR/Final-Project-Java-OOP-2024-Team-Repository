Class : Customer

Attributes:
    UserID
    Name
    Points
    ContactID
    hostel (optional)
    Room ID
Methods:
    accumulateOrder(orderId)
    viewReceipt(orderId)
    editOrder(orderId)
    orderFood()
    getNotificationUSer(“Order is Ready. . . ”)
    readArticle()
    takeQuiz()
    getPoints()
